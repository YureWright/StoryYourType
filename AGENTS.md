## 规则一 — 编码前先思考

明确列出所有预设前提。存在不确定项时，主动询问，杜绝猜测。
需求存在歧义时，主动给出多种解读方案。
若存在更简洁的实现方式，主动提出并推翻复杂方案。
思路混乱时立即暂停，清晰点明存疑问题点。

## 规则二 — 简洁优先原则

编写能解决问题的最小体量代码，不做任何 speculative（前瞻性、冗余）设计。
不新增需求外的功能，不为一次性代码做过度抽象封装。

## 规则三 — 精准改动原则

仅改动必要代码，仅清理自身操作产生的冗余与问题。
禁止擅自优化无关代码、注释、格式排版。
未出现问题的代码，禁止重构，严格贴合项目现有代码风格。

## 规则四 — 目标驱动执行

先定义成功验收标准，循环迭代直至验证通过。
不机械照搬步骤执行，以最终目标为核心自主迭代。
清晰、明确的验收标准，可支撑独立闭环迭代。

## 规则五 — 模型仅用于判断决策

适用场景：分类、文稿起草、总结提炼、信息提取。
禁用场景：路由分发、重试逻辑、确定性数据转换。
可通过代码实现的逻辑，一律由代码实现，不依赖大模型。

## 规则六 — Token上限为硬性约束

单任务上限：4000 Token；单会话上限：30000 Token。
临近上限时，主动总结收尾、重置会话继续工作。
主动上报超限风险，禁止静默超额使用。

## 规则七 — 冲突择优而非折中

出现矛盾的代码范式/规则时，择优选用（更新、更成熟的方案）。
明确说明取舍原因，标记废弃方案待统一清理。
禁止糅合冲突范式，形成不伦不类的折中方案。

## 规则八 — 先通读再编写

新增代码前，务必通读项目导出项、直接调用方、公共工具类。
切勿主观判定代码逻辑互不干扰；若无法理解现有架构设计初衷，主动询问。

## 规则九 — 测试校验核心意图，而非表面行为

测试用例必须体现业务逻辑的核心价值，而非仅校验代码运行结果。
若业务逻辑变更后，测试用例仍不会报错，则该用例设计无效。

## 规则十 — 关键步骤必留检查点

每完成核心步骤，总结已完成内容、已验证内容、待完成内容。
无法清晰复盘当前工作状态时，禁止继续推进。
思路中断、状态混乱时，立即暂停并重新梳理整体进度。

## 规则十一 — 遵从项目现有规范

项目内统一规范，优先级高于个人编码偏好。
若确认现有规范存在缺陷、不合理，可主动提出，禁止私下擅自修改、两套标准并行。

## 规则十二 — 直面问题不隐瞒

若存在静默跳过、未完成的环节，不得标记为「已完成」。
若存在跳过测试用例，不得标记为「测试通过」。
工作存疑、存在不确定性时，优先主动暴露问题，而非隐瞒规避。
